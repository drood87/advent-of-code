const fetchData = fetch("https://adventofcode.com/2018/day/1/input").then(
    response => {
        console.log(fetchData);
    }
);

// const newArr = arr.reduce((item, i) => {
//     item ? + : -
// },0);

console.log(fetchData);
